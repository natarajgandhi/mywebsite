extends default

block content
  div.container
    each person in people
      div.person
        div.person-image(style=`background: url('https://hamster-bucket-nat-arun.s3.amazonaws.com/images/${person.imgSrc}') top center
        no-repeat; background-size: cover;`)
        h2.person-name
          | #{person.firstname} #{person.lastname}
        a(href=`/profile?id=${person.id}`)
          | View Profile

